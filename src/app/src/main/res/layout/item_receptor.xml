<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    style="@style/item_receptor">

    <LinearLayout style="@style/item_receptor_container">

        <ir.webplex.android.automation.ui.CustomTextView
            android:id="@+id/item_receptor_title"
            style="@style/item_receptor_title" />

        <ir.webplex.android.automation.ui.CustomTextView
            android:id="@+id/item_receptor_email"
            style="@style/item_receptor_email" />

    </LinearLayout>

    <RelativeLayout
        android:id="@+id/item_receptor_divider"
        style="@style/item_receptor_divider" />
</RelativeLayout>